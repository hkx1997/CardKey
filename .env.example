# ========== 端口（宿主机映射）==========
APP_PORT=18080
POSTGRES_PORT=5432
REDIS_PORT=6379
FRONTEND_PORT=5173

# ========== 数据库 ==========
POSTGRES_USER=cardkey
POSTGRES_PASSWORD=请改成强密码
POSTGRES_DB=cardkey

# ========== 应用密钥（生产必填，勿用示例值）==========
# ≥32 字符随机串（勿包含 change-me / local-dev / please-rotate）
JWT_SECRET=
# 64 位 hex：openssl rand -hex 32
CONTENT_KEY=

# ========== 首次管理员（可选）==========
# 两者都留空：首次打开管理端走「安装向导」设置账号（推荐）
BOOTSTRAP_ADMIN_USER=
BOOTSTRAP_ADMIN_PASS=

# 公开兑换密钥（可空，向导/启动时自动生成）
PUBLIC_REDEEM_API_KEY=

# ========== 运行 ==========
APP_ENV=production
CORS_ORIGINS=http://localhost:18080,http://127.0.0.1:18080
# HTTPS 反代后建议 true
SECURE_COOKIE=false
TRUST_PROXY=true
# 浏览器 Cookie 写操作同源校验（建议 true）
CSRF_CHECK=true
# 生产建议 true：Redis 不可用则拒绝启动（限流/JWT 吊销依赖）
REQUIRE_REDIS=true
REQUIRE_REDEEM_API_KEY=false
# 空=生产禁用 /metrics；开发放行。设置后请求需 Bearer 或 ?token=
METRICS_TOKEN=

# ========== 在线更新 ==========
# docker=容器仅检测；binary=裸机替换；disabled=关闭
UPDATE_MODE=docker
UPDATE_GITHUB_OWNER=hkx1997
UPDATE_GITHUB_REPO=CardKey
# 建议填写 PAT，避免 GitHub 匿名 API 限流
UPDATE_GITHUB_TOKEN=

# CardKey — 高并发卡密兑换平台 需求文档

## 项目背景

| 项 | 内容 |
|---|---|
| 项目名称 | CardKey（卡密兑换平台） |
| 创建日期 | 2026-07-27 |
| 需求方 | 用户 |
| 技术栈 | 后端 Go · 前端 React · 部署 Docker |
| 状态 | ✅ 已确认（2026-07-27，v1.2 增补类别/共享层/私有仓） |

---

## 需求描述

构建一套**高并发、高安全**的卡密兑换平台：

- **兑换端**：无需登录；用户**先选择卡密类别**，再凭该类别下唯一编码完成兑换
- **管理端**：需登录；**类别管理**、卡密批量导入、查询、作废、统计、API 密钥等
- **开放 API**：卡密录入与兑换均支持 HTTP API；**类别为必传维度**；兑换 API 公开可用
- **类别隔离**：不同类别的卡密**完全不一致**（独立编码空间/前缀规则、独立库存、不可跨类兑换）
- **共享封装**：前后端公共层（响应体、错误码、分页、UI/中间件等）高度统一，业务禁止分叉复制
- **源码**：同步 **私有 GitHub**，**不开源**
- **部署**：开发期全程本地 Docker；生产支持类 sub2api 的一键安装，自动生成管理员并终端输出

---

## 背景和目的

| 维度 | 说明 |
|---|---|
| 解决问题 | 需要一套可自托管、可批量发卡、可 API 对接的卡密兑换系统，支撑高并发兑换场景 |
| 业务价值 | 运营方可快速导入卡密、对外提供兑换入口与 API；用户/第三方系统可零登录完成兑换 |
| 使用场景 | 卡密销售后用户自助兑换；第三方业务系统通过 API 批量录入/兑换；管理员后台运营与审计 |

---

## 功能需求

### 一、兑换端（公开，无需登录）

| # | 功能 | 描述 | 优先级 |
|---|---|---|---|
| R1 | 兑换页面 | **先选类别** → 输入编码 → 提交兑换 → 展示结果 | P0 |
| R2 | 兑换结果展示 | 成功时展示卡密内容；支持一键复制 | P0 |
| R3 | 公开兑换 API | `POST /api/v1/public/redeem`，body **必含 category + code** | P0 |
| R4 | 兑换限流与防护 | IP / (类别+编码) 限流、防爆破 | P0 |
| R5 | 兑换页状态反馈 | 加载中、成功、失败、已使用、已过期等 | P0 |
| R6 | 响应式布局 | 桌面 + 移动端可用 | P1 |
| R7 | 公开类别列表 | config 下发已启用类别（slug/name/prefix/描述） | P0 |

### 一点五、类别管理

| # | 功能 | 描述 | 优先级 |
|---|---|---|---|
| G1 | 类别 CRUD | 名称、slug、**独立 code_prefix**、描述、启用、排序 | P0 |
| G2 | 编码完全不一致 | 每类独立前缀与编码空间；跨类不可兑换 | P0 |
| G3 | 前缀不可变 | 创建后禁止修改 code_prefix / slug（零兼容） | P0 |
| G4 | 卡密归属 | 创建/导入/批次必须绑定类别 | P0 |

### 二、管理端（需登录）

| # | 功能 | 描述 | 优先级 |
|---|---|---|---|
| A1 | 管理员登录 / 登出 | 用户名 + 密码；会话/JWT；登出作废会话 | P0 |
| A2 | 首次安装引导 | 一键安装自动生成管理员账号密码，终端输出；可选强制首次改密 | P0 |
| A3 | 仪表盘 | 卡密总量、未用/已用/作废、今日兑换量、近 N 日趋势等 | P0 |
| A4 | 卡密批量导入 | 支持文本/文件批量导入；每条卡密**自动生成全局唯一兑换编码** | P0 |
| A5 | 卡密单条创建 | 手动录入单条卡密内容 + 类型/批次等元数据 | P0 |
| A6 | 卡密列表与检索 | 分页、按状态/批次/编码/时间筛选；查看详情（含兑换记录摘要） | P0 |
| A7 | 卡密作废 / 启用 | 批量或单条变更状态 | P0 |
| A8 | 导出 | 导出编码列表（含/不含卡密内容，可配置权限） | P1 |
| A9 | 兑换记录 | 查询历史兑换：编码、时间、IP、结果、User-Agent 等 | P0 |
| A10 | API 密钥管理 | 管理端录入类 API 使用 API Key（创建/吊销/限流配置） | P0 |
| A11 | 系统设置 | 站点名称、兑换页文案、限流阈值、是否开启验证码等 | P1 |
| A12 | 操作审计日志 | 管理端关键操作留痕（登录、导入、作废、密钥变更等） | P1 |

### 三、卡密模型（核心业务）

| # | 功能 | 描述 | 优先级 |
|---|---|---|---|
| C1 | 唯一兑换编码 | 系统为每条卡密生成高熵唯一编码（如 `CK-XXXX-XXXX-XXXX` 或 UUID 风格），用户凭此编码兑换 | P0 |
| C2 | 卡密内容 | 兑换成功后向用户展示/返回的载荷（明文存储需加密 at-rest，见安全） | P0 |
| C3 | 状态机 | `unused` → `used` / `disabled` / `expired`；已使用不可再兑 | P0 |
| C4 | 批次（Batch） | 导入时可归属批次，便于运营管理与统计 | P0 |
| C5 | 可选过期时间 | 支持设置过期时间，过期不可兑 | P1 |
| C6 | 卡密类型 | 如 `text` / `account` / `json` 等，影响前端展示形态 | P1 |

**建议字段（草案，设计阶段可调整）：**

```
id, code(唯一兑换编码), content(加密载荷), type, batch_id,
status, expires_at, created_at, used_at, used_ip, note
```

### 四、API 对接

#### 4.1 公开兑换 API（无需管理端鉴权）

| 方法 | 路径（草案） | 说明 |
|---|---|---|
| `POST` | `/api/v1/public/redeem` | Body: `{ "code": "..." }` → 兑换结果 |
| `GET` | `/api/v1/public/health` | 健康检查 |

- 必须有严格限流、可选验证码/挑战、统一错误码（不泄露系统内部信息）
- 同一编码并发兑换必须**幂等且仅成功一次**（事务/乐观锁/分布式锁）

#### 4.2 管理 / 录入 API（需 API Key 或管理员 Token）

| 方法 | 路径（草案） | 说明 |
|---|---|---|
| `POST` | `/api/v1/admin/cards/import` | 批量导入卡密内容，返回生成的 code 列表 |
| `POST` | `/api/v1/admin/cards` | 单条创建 |
| `GET` | `/api/v1/admin/cards` | 列表查询 |
| `PATCH` | `/api/v1/admin/cards/{id}` | 状态变更等 |
| `GET` | `/api/v1/admin/redeems` | 兑换记录 |
| `POST` | `/api/v1/admin/auth/login` | 管理端登录 |
| … | 其余管理能力 | 与管理端 UI 对齐 |

鉴权建议：

- **管理后台 UI**：JWT / Session Cookie（HttpOnly + Secure + SameSite）
- **机器对接录入 API**：`Authorization: Bearer ck_xxx`（API Key，可配置 IP 白名单、权限范围、限流）

### 五、部署与运维

| # | 功能 | 描述 | 优先级 |
|---|---|---|---|
| D1 | 本地 Docker 开发 | `docker compose` 一键起全栈（app + db + redis 等），开发阶段全程使用 | P0 |
| D2 | 生产一键安装 | 类 sub2api：`curl -sSL .../install.sh \| bash`，拉 compose/镜像、生成密钥、初始化 | P0 |
| D3 | 自动管理员账号 | 首次启动若无管理员，自动生成强随机用户名/密码，**打印到终端/日志一次** | P0 |
| D4 | 连接 GitHub 仓库 | 安装脚本、Release 镜像从用户 GitHub 仓库分发（仓库地址后续对接） | P0 |
| D5 | 健康检查 | `/healthz`、`/readyz` | P0 |
| D6 | 配置方式 | 环境变量 + `.env`；敏感项不入库明文配置示例 | P0 |
| D7 | 数据持久化 | 数据库卷、备份指引 | P1 |

**一键安装期望行为（对齐 sub2api）：**

1. 检测 Docker / Compose
2. 下载 `docker-compose.yml`、`.env.example` → 生成 `.env`（JWT 密钥、DB 密码等）
3. `docker compose up -d`
4. 应用首次启动生成管理员凭证并输出：

```text
========================================
  CardKey 安装完成
  管理后台: http://<host>:<port>/admin
  管理员用户: admin_xxxx
  管理员密码: <一次性随机强密码>
  请立即登录并修改密码，妥善保管此输出
========================================
```

### 六、高保真原型（开发前置门禁）

| # | 要求 | 优先级 |
|---|---|---|
| P1 | **先交付可交互高保真原型**，用户确认 UI 后再进入后端/业务开发 | P0 |
| P2 | 原型覆盖：兑换页、登录页、仪表盘、卡密列表、批量导入、兑换记录、API Key 管理 | P0 |
| P3 | UI 风格参考 [chenyme/grok2api](https://github.com/chenyme/grok2api)：现代管理台、深色/浅色、shadcn/ui + Tailwind 气质 | P0 |

> 原型可采用静态 React 前端 + Mock 数据，或 HTML/设计稿级可点击原型；以「可确认布局与交互」为准。

---

## 非功能需求

### 性能与高并发

| 项 | 目标（草案，可调整） |
|---|---|
| 兑换接口 | 单实例支撑高并发兑换；热点编码/IP 限流；DB 行级锁或等价机制保证只兑一次 |
| 延迟 | 正常兑换 P99 < 200ms（局域网/同机 Docker 基准，不含极端网络） |
| 扩展 | 后期支持多实例 + PostgreSQL + Redis（限流/锁/会话） |
| 缓存 | 可选：编码存在性负缓存、配置缓存；**禁止**缓存未使用卡密明文内容到不安全介质 |

### 架构与封装

| 项 | 要求 |
|---|---|
| 后端高度封装 | 分层清晰（handler → service → repository）；领域与基础设施解耦；统一错误码、中间件、配置、日志 |
| 前端高度封装 | 参考 grok2api：`app / features / entities / shared / components/ui`；API 层统一、业务页只组合能力 |
| 同源部署 | 生产由 Go 托管前端静态资源 + API，单端口对外（与 grok2api 类似） |

### 安全性（极高要求）

| 类别 | 措施 |
|---|---|
| 认证 | 管理端强密码策略；JWT 短有效期 + 刷新或会话；API Key 哈希存储（仅展示一次明文） |
| 授权 | 管理 API 与公开 API 严格隔离；最小权限 |
| 卡密内容 | 数据库 at-rest 加密（应用层 AES-GCM 等，密钥来自环境变量）；日志脱敏 |
| 兑换编码 | 高熵、不可预测；防枚举（统一错误响应 + 限流） |
| 传输 | 生产强制 HTTPS 指引；Secure Cookie |
| 注入与 XSS | 参数校验、ORM/参数化查询、前端输出转义 |
| CSRF | Cookie 方案需 CSRF 防护或同站策略 |
| 爆破 | 登录限流、兑换限流、可选验证码/Turnstile |
| 并发安全 | 兑换「只成功一次」强一致；导入幂等/去重策略明确 |
| 审计 | 管理操作与兑换关键事件可追溯 |
| 依赖与镜像 | 最小权限容器、非 root 运行（尽量）、无密钥进镜像 |
| 安装脚本 | 仅从可信 GitHub 源拉取；生成密钥权限 `600` |

### 兼容性

| 项 | 要求 |
|---|---|
| 浏览器 | 近两年 Chrome / Edge / Firefox / Safari |
| 部署 OS | Linux amd64/arm64（生产一键安装）；开发 Windows/macOS Docker 可用 |
| 数据库 | 开发可用 SQLite 或 PostgreSQL；生产推荐 PostgreSQL（高并发） |

### 可观测性

- 结构化日志（请求 ID、脱敏）
- 基础指标预留（兑换 QPS、失败率、延迟）— P1
- 错误不向前端暴露堆栈

---

## UI / UX 风格要求

参考 **grok2api** 管理端气质：

- React + TypeScript + Vite + Tailwind CSS + **shadcn/ui (Radix)**
- 清晰侧边栏布局（管理端）、卡片式仪表盘、表格 + 筛选 + 批量操作
- 深色模式优先，支持浅色切换（与 next-themes 类方案一致）
- 兑换端：极简居中表单，强调信任感与结果可读性（非沉重后台风）
- 中文优先 UI 文案（可预留 i18n）

---

## 成功标准

- [ ] 高保真原型经用户确认通过后再开发
- [ ] 用户凭编码可在兑换端/公开 API 完成兑换，已用卡密不可再次成功
- [ ] 管理端可批量导入并自动生成唯一编码
- [ ] 录入 API（鉴权）与兑换 API（公开）均可对接
- [ ] 本地 `docker compose up` 可完整运行
- [ ] 一键安装脚本可部署并在终端打印管理员账号
- [ ] 安全基线：内容加密、限流、鉴权隔离、并发只兑一次 全部落地
- [ ] 前后端代码结构高度模块化/封装，可维护

---

## 约束条件

| 类型 | 说明 |
|---|---|
| 技术 | Go 后端 + React 前端；Docker 为标准运行方式 |
| 流程 | **原型确认 → 设计确认 → 任务确认 → 开发**（文档门禁） |
| 仓库 | 空仓库起步；GitHub 远程地址由用户提供后对接 CI/Release/安装脚本 URL |
| 范围外（默认不做，除非后续确认） | 支付/商城、多租户 SaaS、移动 App、短信/邮件通知、复杂 RBAC 多角色 |

---

## 风险和假设

| 类型 | 内容 |
|---|---|
| 假设 | 「卡密内容」主要为文本/账号类字符串或 JSON，非大文件分发 |
| 假设 | 初期单管理员即可；多管理员/RBAC 可二期 |
| 假设 | 生产高并发以 PostgreSQL + Redis 为准；单机可用 SQLite 仅适合低并发/演示 |
| 风险 | 公开兑换 API 易被爆破枚举 → 必须限流 + 高熵编码 + 统一错误 |
| 风险 | 一键安装依赖 Docker 与镜像源可达性 |
| 风险 | UI 参考 grok2api 为风格借鉴，非复用其业务代码 |

---

## 待确认问题（请逐条回复）

为避免后续返工，请确认或补充：

1. **卡密「内容」形态**：兑换成功后用户看到的是纯文本、账号密码对、JSON，还是多种类型？是否需要附件/图片？
2. **编码格式偏好**：例如 `CK-A1B2-C3D4-E5F6` 可读格式，还是纯随机 UUID/hex？
3. **数据库选型**：开发/生产是否统一 PostgreSQL + Redis？还是开发 SQLite、生产 PG？
4. **验证码**：兑换页是否必须接 Cloudflare Turnstile / hCaptcha？（对防爆破很关键）
5. **GitHub 仓库地址**：是否已有仓库 URL？安装脚本将绑定该仓库 raw/Release。
6. **多管理员**：一期是否只支持单一管理员账号？
7. **兑换后是否允许「查询历史」**：用户仅兑换当下看一次结果，还是凭编码可重复查询已兑结果？
8. **导入格式**：纯一行一条内容、CSV（content,note）、还是 JSONL？
9. **项目对外名称**：正式名用 `CardKey` 还是其他品牌名？
10. **原型形式偏好**：可交互 React Mock 原型（推荐，与正式前端同栈）还是静态 HTML？

---

## 推荐默认（若您未特别指定将按此执行）

| 项 | 默认 |
|---|---|
| 卡密内容 | 文本为主，支持 `type=text\|json` |
| 编码格式 | 高熵可读：`CK-` + 分组 Base32/hex |
| 数据层 | PostgreSQL + Redis（开发 Docker 一并拉起） |
| 验证码 | 配置项，默认开启简易图形或 Turnstile 占位，生产可配密钥 |
| 管理员 | 一期单管理员 + 强制/提示改密 |
| 已兑查询 | 允许凭编码再次查询「已兑换结果」（需限流），避免用户刷新丢失 |
| 导入 | 支持 TXT 一行一条 + CSV + JSON 数组 |
| 项目名 | CardKey |
| 原型 | React + shadcn 可交互高保真（Mock API） |
| 后端 | Go（Gin 或 Fiber 或 Echo，设计阶段选定）+ 清晰分层 |
| 前端 | 对齐 grok2api：React19 + Vite + Tailwind4 + shadcn + TanStack Query |

---

## 交付阶段（确认需求后执行）

```
阶段 0  需求文档确认          ← 当前
阶段 1  设计文档 DESIGN.md   （架构/数据/API/安全/部署）→ 确认
阶段 2  任务文档 TASKS.md    → 确认
阶段 3  高保真 UI 原型        → 您确认 UI
阶段 4  开发（Docker 本地）+ 安全与 API
阶段 5  一键安装脚本 + GitHub Release 对接
```

---

**请确认：本需求文档是否准确？**  
如有修改请直接指出条款编号；若整体认可，请回复「需求确认」并尽量回答上方「待确认问题」。确认通过后，才会进入 `DESIGN.md` 设计阶段。
